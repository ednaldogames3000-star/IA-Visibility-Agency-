{
  "name": "costa-visibility-agency",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check lib/audit.js && node --check lib/auth.js && node --check server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "better-sqlite3": "^11.7.0"
  }
}
